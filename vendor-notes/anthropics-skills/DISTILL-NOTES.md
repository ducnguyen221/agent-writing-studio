# Ghi chú chưng cất skills

## Đã chưng cất

| Nguyên lý upstream | Cách chuyển hóa trong studio |
|---|---|
| Thu thập bối cảnh trước khi viết | `references/01-phong-van-boi-canh.md` của trục 1: hỏi theo `intent_questions[]` của §1, dừng khi gỡ hết `stop_if_missing` |
| Thử bài bằng cách giả lập độc giả | `references/02-reader-testing.md`: độc giả theo `audience.schema` đọc outline và trả về ba câu họ sẽ hỏi |

## Không mang sang

- Câu chữ của `SKILL.md` — repo không có license nên không được chép.
- Cấu trúc frontmatter và quy ước đóng gói riêng của upstream.
- Mọi skill khác trong repo, kể cả các skill xử lý tài liệu.

## Ranh giới

KIỂM TAY 30/08/2026: gốc repo KHÔNG có file `LICENSE`, và thư mục `skills/doc-coauthoring/` chỉ chứa đúng một file `SKILL.md`, không kèm license riêng. `THIRD_PARTY_NOTICES.md` chỉ nói về thư viện bên thứ ba, không cấp phép cho nội dung repo. Kết luận: `idea-only`. Hai ý lấy về đều là thực hành phổ biến của nghề biên tập và được viết lại từ đầu.
