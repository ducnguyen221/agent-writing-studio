# `docs/` — tài liệu của repo

Bốn tài liệu dưới đây đi cùng repo: ai clone hay cài plugin đều có chúng.

| File | Trả lời câu hỏi gì |
|---|---|
| [`KIEN-TRUC.md`](KIEN-TRUC.md) | Vì sao là 5 skill × dữ liệu thể loại chứ không phải 25 skill? Skill nào đọc dữ liệu của ai, file nào là nguồn sự thật của cái gì? |
| [`CHAM-DIEM.md`](CHAM-DIEM.md) | Điểm dấu hiệu **S** và độ phủ **C** là gì, đo bằng cách nào, ngưỡng nào ứng với hành động nào, và mẫu báo cáo giám định trông ra sao. |
| [`GENRES.md`](GENRES.md) | Cách soạn một hồ sơ thể loại mới — năm mục bắt buộc, viết gì trong mỗi mục, kiểm bằng test nào. |
| [`agent-writing-studio.md`](agent-writing-studio.md) | Tầm nhìn gốc của chủ repo: ma trận 5 giai đoạn × 5 loại hình, viết trước khi có dòng code nào. |

## Hai thư mục không có ở đây

`plans/` và `results/` là **nhật ký làm việc của xưởng**: spec từng đợt, danh sách task, nhật ký cổng
duyệt, và các kết quả đo thô. Chúng chỉ nằm trên máy tác giả — `.gitignore` chặn chúng khỏi repo
public.

Lý do không phải vì chúng bí mật, mà vì chúng là **ghi chép quy trình**, không phải tài liệu người
dùng: chúng nói "ngày 30/08 cổng B chưa qua vì thiếu X" chứ không nói repo này dùng thế nào. Những
con số và kết luận đáng giữ từ đó đã được viết lại vào README, `KIEN-TRUC.md` và `CHAM-DIEM.md`.

Vì thế, khi một tài liệu trong repo nhắc tới "nhật ký cổng nội bộ" hay "sổ đo nội bộ của đợt xây",
đó là con trỏ tới hai thư mục này — cố ý không đặt thành liên kết, vì liên kết đó sẽ chết trên
GitHub.
